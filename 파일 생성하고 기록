#include <stdio.h>
int main(void)
{
FILE *fp;
fp = fopen(“sample.txt", "w");
if (fp == NULL)
{
printf("Write Error!!\n");
return 0;
}
fputs("Hello World!! \n", fp);
fputs("Hello World!! \n", stdout);
fclose(fp);
return 0;
}
